# MCP Integration

The primary MCP documentation now lives in `docs/mcp/`.

- Service overview: `docs/mcp/README.md`
- Claude Desktop examples: `docs/mcp/claude_desktop.md`

The repository-level MCP helper scripts are still in `mcp/`.
